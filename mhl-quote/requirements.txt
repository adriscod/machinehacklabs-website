# Core (STL + cost model). Enough to quote an STL on Windows.
numpy>=1.24
numpy-stl>=3.1
PyYAML>=6.0

# Optional: STEP import (preferred CAD format).
#   pip install -r requirements-step.txt
# or:
#   pip install "cadquery>=2.4"
